// Basic UI components placeholder
// This will be expanded later with actual UI components

export const createButton = (text: string) => {
  return `<button>${text}</button>`;
};

export const createInput = (placeholder: string) => {
  return `<input placeholder="${placeholder}" />`;
};